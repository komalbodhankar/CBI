import { useEffect } from 'react';
import useUpdate from 'react-use/lib/useUpdate.js';

var _useUpdate = (useUpdate === null || useUpdate === void 0 ? void 0 : useUpdate.__esModule) ? useUpdate['default'] : useUpdate;

export default function useUpdaters(memoized, args) {
  var update = _useUpdate();

  useEffect(function () {
    var updaters = memoized.updaters;
    updaters.add(update);
    return function () {
      updaters.delete(update);
    }; // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [memoized]);
}
//# sourceMappingURL=useUpdaters.js.map